<UserControl x:Class="Antyplagiat.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White"
             Height="850" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="600">

            <TextBlock Text="Postęp analizy" 
                       FontSize="36" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,40"/>

            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="18" Foreground="#555"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <ProgressBar Value="{Binding ProgressValue}" 
                        Height="20" 
                        Minimum="0" Maximum="100"
                        Foreground="#007ACC" Background="#E0E0E0"
                        BorderThickness="0"
                        ClipToBounds="True" 
                        >
            </ProgressBar>

            <TextBlock Text="{Binding ProgressValue, StringFormat={}{0}%}" 
                       HorizontalAlignment="Center" Margin="0,5,0,0"/>

            <Button Content="Pokaż raport"
                    Command="{Binding ShowResultCommand}"
                    IsEnabled="{Binding IsAnalysisFinished}"
                    FontSize="16" FontWeight="Medium" Foreground="White"
                    Background="#1f1f1f" 
                    Padding="10" Margin="0,50,0,0" Height="50" Width="200">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

        </StackPanel>
    </Grid>
</UserControl>